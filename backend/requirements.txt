# =============================================================================
# TriagePlus v4 - Backend dependencies (CPU/development install)
#
# For CUDA/GPU acceleration see requirements-gpu.txt.
# Pinned versions keep builds reproducible; bump intentionally with CI.
# =============================================================================

# --- Web framework -----------------------------------------------------------
fastapi==0.109.0
uvicorn[standard]==0.27.0
websockets==12.0
python-multipart==0.0.6

# --- Settings / validation ---------------------------------------------------
pydantic==2.6.0
pydantic-settings==2.2.0
pydantic-extra-types==2.6.0

# --- Database ----------------------------------------------------------------
supabase==2.3.4
aiosqlite==0.19.0              # transitive dep of langgraph-checkpoint-sqlite, pinned for reproducibility

# --- LangGraph (conversational state machine) --------------------------------
langgraph==0.2.0
langgraph-checkpoint-sqlite>=2.0,<3.0   # note: 1.x never shipped on PyPI; 2.x keeps the SqliteSaver.from_conn_string API

# --- LangChain (used only for the Ollama prompt path in triage_graph.py) -----
langchain-ollama==0.2.0
langchain-core==0.2.0

# --- LLM / NER ---------------------------------------------------------------
transformers==4.38.0
accelerate==0.28.0
torch>=2.1,<3.0                # Required by transformers' NER pipeline + sentence-transformers
sentence-transformers==3.0.0

# --- Retrieval (hybrid BM25 + Dense FAISS) -----------------------------------
faiss-cpu>=1.9,<2.0               # note: 1.8.x never shipped on PyPI (closest are 1.7.4 wheel-only and 1.9.0)
rank-bm25==0.2.2

# --- Knowledge graph ---------------------------------------------------------
networkx==3.2.1

# --- ML classifier (DDXPlus triage) -----------------------------------------
xgboost==2.0.3
scikit-learn>=1.4,<2.0          # 1.4.x has no Py3.13 wheel; >=1.4 covers the API used (MultiLabelBinarizer, LabelEncoder)
scipy>=1.11,<3.0               # Sparse matrices used by triage_graph + train_xgboost
pandas>=2.1,<3.0
numpy>=1.24,<2.0

# --- Fuzzy matching ----------------------------------------------------------
rapidfuzz==3.9.0

# --- HTTP client (Ollama health check in main.py) ----------------------------
httpx==0.26.0
requests>=2.31,<3.0

# --- Env-file loader (used by seed_doctor.py) --------------------------------
python-dotenv>=1.0,<2.0

# --- Dev tooling -------------------------------------------------------------
pytest==8.0.0
pytest-asyncio==0.23.0
ruff==0.3.0
